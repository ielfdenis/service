# Обзор проекта - PPTX Template Processing System

## Что было создано

Полнофункциональная система для работы с PowerPoint шаблонами на Spring Boot с использованием Apache POI.

## Файловая структура

```
reporting/
├── src/main/java/com/example/reporting/
│   ├── ReportingApplication.java                # Точка входа Spring Boot
│   ├── controller/
│   │   └── PptxController.java                  # REST API endpoints
│   ├── service/
│   │   ├── PptxReaderService.java              # Чтение и парсинг PPTX
│   │   ├── PptxModificationService.java        # Координация замены
│   │   ├── PptxDownloadService.java            # Подготовка к скачиванию
│   │   └── replacer/
│   │       ├── PlaceholderReplacer.java        # Интерфейс стратегии
│   │       ├── TextPlaceholderReplacer.java    # Замена текста
│   │       └── ImagePlaceholderReplacer.java   # Замена изображений
│   ├── model/
│   │   ├── Placeholder.java                    # Модель плейсхолдера
│   │   ├── PlaceholderType.java                # Enum типов (TEXT/IMAGE)
│   │   ├── TemplateData.java                   # DTO для API
│   │   └── ImageData.java                      # Данные изображения
│   ├── util/
│   │   └── ImageUtils.java                     # Утилиты для изображений
│   └── exception/
│       └── TemplateProcessingException.java    # Кастомное исключение
│
├── src/main/resources/
│   ├── templates/                              # Папка для PPTX шаблонов
│   │   └── README.md                           # Инструкции по шаблонам
│   └── application.properties                  # Конфигурация
│
├── src/test/java/com/example/reporting/
│   ├── ReportingApplicationTests.java
│   └── service/
│       └── TextPlaceholderReplacerTest.java    # Unit тесты
│
├── pom.xml                                     # Maven зависимости
├── CLAUDE.md                                   # Документация для Claude Code
├── README_RU.md                                # Руководство на русском
├── PPTX_USAGE.md                               # Детальное руководство
├── ARCHITECTURE.md                             # Описание архитектуры
├── API_EXAMPLES.http                           # Примеры HTTP запросов
└── PROJECT_SUMMARY.md                          # Этот файл
```

## Ключевые возможности

### 1. Универсальный парсинг шаблонов
- Автоматическое обнаружение плейсхолдеров `{{key}}`
- Работа с любым PPTX шаблоном
- Поддержка множественных плейсхолдеров

### 2. Замена текста
- Поиск и замена текстовых плейсхолдеров
- Сохранение форматирования
- Поддержка множественных вхождений

### 3. Замена изображений
- Замена текстовых плейсхолдеров на изображения
- Сохранение позиции и размера
- Поддержка форматов: PNG, JPEG, GIF, BMP, TIFF

### 4. REST API
- Извлечение плейсхолдеров из шаблона
- Генерация презентации с скачиванием
- Модификация и получение байтов

### 5. Расширяемая архитектура
- Strategy Pattern для типов плейсхолдеров
- Легкое добавление новых типов
- Spring DI для автоматической регистрации

## Технологический стек

| Технология | Версия | Назначение |
|------------|--------|------------|
| Spring Boot | 4.0.0 | Основной фреймворк |
| Apache POI | 5.2.5 | Работа с PPTX |
| Java | 17 | Язык программирования |
| Lombok | Latest | Уменьшение boilerplate |
| JUnit 5 | Latest | Тестирование |
| Maven | Latest | Сборка проекта |

## API Endpoints

### GET /api/pptx/placeholders
Извлекает список плейсхолдеров из шаблона

**Параметры:**
- `templatePath` - путь к шаблону в resources

**Ответ:**
```json
["companyName", "reportTitle", "logo"]
```

### POST /api/pptx/generate
Генерирует презентацию и возвращает файл для скачивания

**Тело запроса:**
```json
{
  "templateName": "templates/report.pptx",
  "placeholders": [...]
}
```

**Ответ:** Файл PPTX для скачивания

### POST /api/pptx/modify
Модифицирует презентацию и возвращает байты

**Тело запроса:** Аналогично `/generate`

**Ответ:** byte[] в формате PPTX

## Примеры использования

### 1. Подготовка шаблона

Создайте PPTX в PowerPoint:
```
Slide 1:
  Заголовок: "{{title}}"
  Текст: "Отчет для {{companyName}}"
  Логотип: Текстовое поле с "{{logo}}"
```

Сохраните в `src/main/resources/templates/report.pptx`

### 2. API вызов

```bash
curl -X POST http://localhost:8080/api/pptx/generate \
  -H "Content-Type: application/json" \
  -d '{
    "templateName": "templates/report.pptx",
    "placeholders": [
      {"key": "title", "type": "TEXT", "value": "Q4 Report"},
      {"key": "companyName", "type": "TEXT", "value": "Acme Corp"}
    ]
  }' \
  --output result.pptx
```

### 3. Java код

```java
@Autowired
private PptxModificationService service;

public void generate() throws Exception {
    TemplateData data = TemplateData.builder()
        .templateName("templates/report.pptx")
        .placeholders(List.of(
            Placeholder.builder()
                .key("title")
                .type(PlaceholderType.TEXT)
                .value("Annual Report")
                .build()
        ))
        .build();

    XMLSlideShow pres = service.modifyPresentation(data);
}
```

## Расширение функциональности

### Добавление нового типа (например, таблицы)

1. Добавьте тип в enum:
```java
public enum PlaceholderType {
    TEXT, IMAGE, TABLE
}
```

2. Создайте replacer:
```java
@Component
public class TablePlaceholderReplacer implements PlaceholderReplacer {
    public boolean canHandle(Placeholder p) {
        return p.getType() == PlaceholderType.TABLE;
    }

    public void replace(XMLSlideShow pres, Placeholder p) {
        // Логика замены таблицы
    }
}
```

Готово! Spring автоматически найдет и зарегистрирует новый replacer.

## Команды для разработки

```bash
# Установка зависимостей
./mvnw clean install

# Запуск приложения
./mvnw spring-boot:run

# Запуск тестов
./mvnw test

# Сборка JAR
./mvnw package

# Запуск конкретного теста
./mvnw test -Dtest=TextPlaceholderReplacerTest
```

## Документация

- **CLAUDE.md** - Руководство для Claude Code
- **README_RU.md** - Быстрый старт на русском
- **PPTX_USAGE.md** - Детальное руководство по использованию
- **ARCHITECTURE.md** - Техническая архитектура
- **API_EXAMPLES.http** - Готовые HTTP запросы

## Преимущества решения

✅ **Универсальность** - работает с любым PPTX шаблоном
✅ **Расширяемость** - легко добавлять новые типы данных
✅ **Простота** - интуитивный API и понятная структура
✅ **Тестируемость** - модульная архитектура
✅ **Production-ready** - логирование, обработка ошибок

## Следующие шаги

### Возможные улучшения

1. **Асинхронная обработка** - для больших файлов
2. **Кэширование** - шаблонов и результатов
3. **Batch processing** - множественная генерация
4. **Validation** - проверка входных данных
5. **Metrics** - мониторинг производительности
6. **Таблицы** - поддержка замены таблиц
7. **Диаграммы** - динамические графики

### Интеграции

- **Database** - хранение шаблонов в БД
- **S3/Cloud Storage** - для больших файлов
- **Message Queue** - асинхронная обработка
- **Authentication** - Spring Security
- **API Gateway** - для microservices

## Контакты и поддержка

При возникновении вопросов:
1. Проверьте PPTX_USAGE.md
2. Изучите примеры в API_EXAMPLES.http
3. Посмотрите тесты в src/test/

---

**Система готова к использованию!** 🚀

Запустите приложение командой `./mvnw spring-boot:run` и начните работу с PPTX шаблонами через REST API.